const longestString = (strArr) => {
  // tempStr variable to hold the string
  // loop over strArr
  // if current string in arry is bigger than tempStr then update the variable tempStr
  // return tempStr;
};

console.log(longestString(['abc', 'def', 'gasdfasf', 'asdf', 'e', 'agwoaiengpoing']));